# מה המספרים אומרים, ומה הם לא אומרים

מסמך אחד שמסביר את המודל. אם משהו כאן סותר את הקוד — הקוד צודק והמסמך שגוי,
ויש לתקן אותו. הגדרות מספריות (סף, אופק, עומק) חיות ב-`cvs.py` וב-`stress.py`
בלבד ונכתבות ל-`score_meta.json` בכל הרצה — כאן הן מוסברות, לא מוגדרות.

## שלושת המספרים, ולמה קל להתבלבל

`score.csv` מכיל שלוש עמודות שנשמעות דומה. **שמות העמודות מטעים** ואי אפשר
להסיק מהם מה נמצא בפנים:

| עמודה | מה זה באמת | טווח | האם זו הסתברות |
|---|---|---|---|
| `percentile` | ה-MSS הגולמי: ממוצע האחוזונים של הגורמים שנבחרו | 0–100 | **לא** |
| `chance_pct` | הקריאה המכוילת — כמה מתוך מאה ימים כאלה נפלו | ~5–50 | **כן** |
| `score` | מיקום התצוגה של `chance_pct` בתוך הטווח שעקומת אותה שנה משתרעת עליו | 0–100 | **לא** |

- **MSS** הוא הציון הגולמי. הוא הבסיס לחלוקת המצבים (regime), והוא הסולם היחיד
  שמשמעותו זהה לאורך כל ההיסטוריה.
- **`chance_pct`** הוא המספר היחיד שנמדד בהסתברות. הוא נקרא מעקומת כיול.
- **`score`** קיים רק כדי שמחוון ויזואלי ינצל את כל הרוחב שלו. אין לו משמעות
  מחוץ לשנה שבה חושב.

`check.py` מדפיס את MSS ואת `chance_pct`. את `score` הוא לא מדפיס בכוונה.

## הגדרת האירוע

> הסגירה הנמוכה ביותר ב-20 המפגשים הבאים, **החל מהמפגש שאחרי הקריאה**, ברמה של
> ‎-5% או פחות מסגירת היום.

בפירוט, כי כל מילה כאן משנה את המספר:

- **סגירה מול סגירה.** לא שיא-לשפל, לא תוך-יומי. נפילה של 5% שהתרחשה והתאוששה
  באותו יום אינה אירוע.
- **20 מפגשי מסחר**, לא 20 ימי לוח.
- **יום הקריאה עצמו אינו בחלון.** הקוד עושה זאת ב-`fwd_drawdown` דרך
  `.shift(-1)`. הסיבה: הקריאה מחושבת על סגירת היום, ולכן אי אפשר לפעול לפניה.
- 20 השורות האחרונות של הסדרה נזרקות מכל טבלת שיעורים, כי חלון הקדימה שלהן
  גולש מעבר לסוף הנתונים. ההטיה הזו רצה **לשני הכיוונים** — ראו ההערה
  ב-`event_rate` — ולכן הכלל הוא לזרוק, לא לתקן.

## מאיפה 15.8%

זה שיעור האירועים בכל ימי ה-walk-forward, אחרי זריקת הזנב: 967 אירועים מתוך
6,122 ימים, 1999–2026. זה לא קבוע — הוא מחושב מחדש בכל הרצה ונכתב ל-
`score_meta.json` תחת `baseline_rate`. אף קובץ אחר לא מחזיק את המספר הזה.

זה גם המודל היריב: אמירת "15.8% כל יום" היא תחזית לגיטימית, וכל מספר של המודל
נמדד מולה ב-`eval.py`.

## תקופות: fitted מול walk-forward

- ההיסטוריה מתחילה 1991 (`START = "1990-01-01"` פחות תקופת החימום).
- העמודה `walk_forward` היא `False` עד 1999-01-04. באותה תקופה הגורמים נבחרו
  ותוכיילו על נתונים שכוללים את אותם ימים. **הימים האלה אינם ראיה.** הם מוצגים
  כדי שהגרף לא ייחתך, ו-`check.py` מוסיף אזהרה אם הקריאה הנוכחית נופלת שם.
- מ-1999 והלאה: הגורמים נבחרים מחדש בכל ינואר על שנים קודמות בלבד, ועקומת
  הכיול לכל שנה נבנית מימי walk-forward שקדמו לה בלבד. שנתיים ראשונות של חימום
  קוראות מהעקומה הראשונה שקיימת — זה החריג היחיד.
- `eval.py` מודד רק על ימי walk-forward. מדידה על התקופה המותאמת היא מדידה מול
  פתרון הבחינה.

## שיטת הכיול

`calibration()` לוקחת את ימי ה-walk-forward שקדמו לשנה הנמדדת, מחלקת אותם
ל-10 קבוצות שוות בגודלן לפי MSS (`pd.qcut`), ושואלת איזה חלק מכל קבוצה לוו
באירוע. העקומה נאכפת **עולה בלבד** (`cummax`): יותר לחץ לא יכול למפות לפחות
סיכון, וירידה בעקומה היא מדגם דליל ולא אמירה של השוק. קריאה של ציון נעשית
באינטרפולציה לינארית, ושטוחה מחוץ לטווח העקומה.

האוכלוסייה: ימי מסחר בארה"ב, S&P 500 בלבד. אין כאן מניות בודדות, אין שווקים
אחרים, ואין תיקון להטיית שרידות — כי אין universe של מניות מלכתחילה, רק מדדים
ותעודות סל שקיימות היום. זו מגבלה אמיתית: תעודת סל שנסגרה לא נמצאת בנתונים.

`bucket_days` ב-`score_meta.json` הוא מספר הימים בקבוצת הכיול שהקריאה הנוכחית
נופלת בה. `check.py` מדפיס ממנו טווח 95%, ומסמן `LOW CONFIDENCE` מתחת ל-100
ימים. שיעור שנמדד על קומץ אירועים הוא סיפור, לא הסתברות מכוילת.

## מצבים (regime) — סיווג, לא הסתברות

`WATCH / ELEVATED / CRITICAL` הם חיתוך של ה-MSS לפי ספים, עם היסטרזיס: נכנסים
בסף העליון ויוצאים רק מתחת לסף התחתון, כדי שקריאה שמרחפת על הגבול לא תהבהב.

**מקור האמת היחיד הוא `cvs.BANDS`.** אין להעתיק את המספרים לשום מקום אחר —
`score_meta.json` מייצא אותם משם, ו-`test_check.py` בודק כל סף מנקודה מעל
ומנקודה מתחת. `FLAG = 85` ב-`stress.py` הוא סף דגל המכירה, והוא סף אחד ולא
טווח.

מדיניות שינוי ספים:

1. אין לכוונן סף לפי הקריאה של היום. סף שנבחר כי הוא "מרגיש נכון עכשיו" הוא
   התאמת יתר לנקודה אחת.
2. כל שינוי בספים או בשיטת הכיול מחייב העלאת `MODEL_VERSION` ב-`stress.py`.
3. כל שינוי כזה נרשם ב-[CALIBRATION.md](CALIBRATION.md) עם התאריך, הסיבה
   והמספרים לפני ואחרי.

## מה נמדד, ומה נמצא

`python eval.py` מפיק את הטבלאות. שתי שאלות נפרדות, שלא נכון לערבב:

- **הבחנה (discrimination)** — האם קריאה גבוהה באמת מקדימה יותר נפילות. נמדד
  ב-AUC.
- **כיול (calibration)** — האם "20%" באמת אומר עשרים אחוז. נמדד ב-Brier ובעקומת
  מהימנות.

מודל יכול לדרג ימים מצוין ולטעות בכל הרמות, או להיות מדויק בממוצע ולדרג כמו
מטבע. הממצאים הנוכחיים (walk-forward 1999–2026):

| מדד | המודל | הבסיס הקבוע 15.8% |
|---|---|---|
| Brier | 0.1470 | **0.1330** |
| AUC על MSS | 0.644 | 0.5 |
| AUC על `chance_pct` (מאוחד) | 0.547 | 0.5 |

שלוש מסקנות, כולן לא נוחות ולכן כתובות כאן:

1. **ה-MSS מבחין.** 0.644 הוא לא רעש, וזה נראה גם בטבלת המצבים: 35.6% אירועים
   ב-CRITICAL מול 9.0% ב-NORMAL, פי ארבעה.
2. **`chance_pct` המאוחד מבחין הרבה פחות (0.547).** זו תכונה של המבנה, לא של
   הגורמים: העקומה מצוירת מחדש בכל ינואר, ולכן אותו MSS ממופה לאחוז אחר ב-2003
   וב-2026. דירוג של כל השנים יחד משווה קריאות שמעולם לא היו על אותו סולם.
   בתוך תקופה בודדת ה-AUC חוזר לרמת ה-MSS.
3. **הכיול עצמו גרוע מהבסיס הקבוע.** ה-Brier של המודל (0.1470) גבוה מזה של
   "15.8% כל יום" (0.1330), והקבוצות העליונות בעקומת המהימנות מבטיחות הרבה יותר
   ממה שהתרחש (קבוצה 9: הבטיחה 50%, קרו 15.6%). כלומר: **הרמה המוחלטת של
   `chance_pct` אינה אמינה כרגע**, גם כשהדירוג שמאחוריה כן. עד שזה יתוקן, המספר
   הנכון להסתכל עליו הוא היחס לבסיס ולא האחוז המוחלט.

זה לא באג בקוד המדידה — `eval.py --selftest` בודק את כל המדדים על סדרות
שהתשובה שלהן ידועה בבנייה. זו תוצאה של המודל.

## הערכת סיכון אינה הוראת פעולה

`chance_pct` הוא אומדן של מה שקרה בעבר אחרי ימים שנראו כמו היום. הוא אינו
המלצה לקנות, למכור או להחזיק, והמסמך הזה אינו ייעוץ השקעות.

הפרדה מכוונת בין שתי שכבות:

- **שכבת הערכה** — `stress.py`, `check.py`, `eval.py`. מוציאה מספרים והסתייגויות.
- **שכבת פעולה** — לא קיימת כאן. אם תיבנה אי פעם, היא שכבה נפרדת עם סף מוצהר,
  עלות עסקה, וגודל פוזיציה, והיא תימדד בנפרד.

הטבלה האחרונה ב-`eval.py` מציגה precision/recall מול סף מוצהר (ברירת מחדל 25%)
**כדיאגנוסטיקה בלבד** — כדי להראות מה כלל כזה היה עולה. ב-1,654 ימים שהיו
חוצים 25%, ה-precision הוא 0.15 ו-1,406 מהם היו התרעות שווא. זו הסיבה שהסף הזה
אינו כלל מסחר בקוד.
