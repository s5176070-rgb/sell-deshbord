# בדיקת סיכוי לנפילה

## הפקודה היחידה שצריך

```bash
python check.py
```

מוריד מחירים, מחשב מחדש, ומדפיס בעברית: תאריך הנתון, אחוז סיכוי, ציון גולמי, מצב, מגמה, מקור, גרסת מודל וסנטימנט CNN.

אם הרענון נכשל — הפקודה נכשלת ולא מדפיסה כלום. קריאה שמורה מוצגת רק כשמבקשים אותה במפורש:

```bash
python check.py --offline
```

אותו סיכום מהקריאה השמורה (`score.csv`), בלי הורדות — מיידי.

קוד יציאה: `0` תקין, `1` כשל רענון / קובץ פגום / נתון ישן מ-5 ימים (במקרה הזה גם מודפסת שורת אזהרה).

הניסוח של "5% ב-20 מפגשים", הבסיס ההיסטורי וגרסת המודל נקראים מ-`score_meta.json` ש-`stress.py` כותב ליד `score.csv` — לא כתובים קשיח.

```bash
python check.py --selftest    # בדיקות הוולידציה, בלי רשת
```

## פקודות המקור (אם רוצים את הפירוט המלא)

```bash
python stress.py --bench    # כל הטבלאות: walk-forward, buckets, יציבות. לא כותב כלום
python stress.py --full     # דף HTML עם כל הראיות
python stress.py --serve    # דף חי עם כפתור רענון
python breadth.py           # בונה מחדש את נתוני הרוחב (איטי)
```

## כמה המספר עצמו שווה

```bash
python eval.py              # Brier, AUC, עקומת מהימנות, מול בסיס קבוע
python eval.py --offline    # אותו דבר מ-spx.csv השמור, בלי הורדה
python eval.py --selftest   # המדדים על סדרות שהתשובה שלהן ידועה
```

הממצאים והמגבלות מוסברים ב-[MODEL.md](MODEL.md), ובעיקר: הדירוג של ה-MSS עובד,
אבל הרמה המוחלטת של `chance_pct` כרגע פחות מדויקת מניחוש קבוע של 15.8%.

## דוגמת פלט

דוגמה היסטורית מ-08.09.2026, לא המצב הנוכחי. להרצה עכשיו — `python check.py`.

```
— הערכת סיכון —
תאריך הנתון: 08.09.2026
סיכוי לנפילה של 5% ב-20 מפגשים: ~20% (טווח 16–23%, מדגם 595 ימים)
בסיס היסטורי לכל יום: 16%  —  פי 1.24 מהבסיס, +4 נקודות אחוז
ציון גולמי (MSS): 56 מתוך 100 — ממוצע אחוזוני הגורמים, לא הסתברות
מצב: WATCH (סיווג לפי סף על ה-MSS, לא הסתברות)
מגמה מול 5 תצפיות אחורה: +4
מקור: קריאה שמורה (--offline)
גרסת מודל: 2026.09   חושב: 2026-09-09T11:43:58+03:00
— זו הערכת סיכון בלבד, לא הוראת פעולה —
סנטימנט CNN: 39 (09.09) — להקשר בלבד, לא בציון
```

באותה קריאה היסטורית: סיכון בינוני-עולה, ללא איתות מכירה. אין בכך אמירה על היום,
ואין בכך המלצה לפעולה — ראו [MODEL.md](MODEL.md).
